## 现状结论（你没找到新增选项的原因）
- **后端已实现新增/编辑/删除**：
  - `POST /cashier/serviceItem/add`、`PUT /cashier/serviceItem/update`、`DELETE /cashier/serviceItem/delete`、`DELETE /cashier/serviceItem/deleteBatch`（见 [ServiceItemController.java](file:///Users/lv/Workspace/erp/jshERP-boot/src/main/java/com/jsh/erp/controller/ServiceItemController.java#L69-L135)）。
  - 对应业务在 [ServiceItemService.java](file:///Users/lv/Workspace/erp/jshERP-boot/src/main/java/com/jsh/erp/service/cashier/ServiceItemService.java#L31-L66)。
- **前端没有“服务项目管理/新增”页面或入口**：目前“项目列表”只用于收银下单选择（见 [CashierDeskModal.vue](file:///Users/lv/Workspace/erp/jshERP-web/src/views/cashier/modules/CashierDeskModal.vue)），因此你在收银弹窗里看不到“新增项目”。
- **重要问题（需要顺手修复）**：`jsh_service_item` 的 MyBatis `insert/insertSelective` 没有写入 `tenant_id`（见 [ServiceItemMapper.xml](file:///Users/lv/Workspace/erp/jshERP-boot/src/main/resources/mapper_xml/ServiceItemMapper.xml#L31-L85)）。这会导致“非 admin 租户用户新增的项目”因为列表按 `tenant_id` 过滤而**看不见**。

## 拟实现目标
- 提供一个明确可用的“服务项目管理”入口：可列表、查询、启用/禁用、新增、编辑、删除。
- 新增/编辑的数据能被当前租户正常查询到（修复 `tenant_id` 写入）。
- 菜单可见且权限体系一致（沿用现有 `jsh_function + RoleFunctions(btn_str)` 机制）。
- 按你之前要求：新增功能同步补充到统一使用文档末尾。

## 设计方案（前端 + 后端 + 数据库）
### 1）数据库（无需改表结构，仅补充菜单/权限数据）
- 业务表 **不新增**：服务项目仍存于 `jsh_service_item`。
- 新增一条菜单记录到 `jsh_function`（建议挂在“收银”下）：
  - `parent_number='0900'`（收银）
  - `url='/cashier/service_item'`
  - `component='/cashier/ServiceItemList'`
  - `push_btn='1'`（与现有列表页一致，控制按钮码）
- 更新 `jsh_user_business` 的 `RoleFunctions`：把新菜单 funId 追加到你们常用角色（例如示例库里“测试用户”是 roleId=10）并在 `btn_str` 里追加 `{funId: 新id, btnStr:'1'}`。
- 同时提供一份可重复执行的升级脚本放到 `jshERP-boot/docs/`（例如 `upgrade_service_item_manage_menu.sql`）。

### 2）后端（修 bug + 可选增强）
- **必须修复**：在 [ServiceItemMapper.xml](file:///Users/lv/Workspace/erp/jshERP-boot/src/main/resources/mapper_xml/ServiceItemMapper.xml) 的 `insert` 和 `insertSelective` 中补上 `tenant_id` 字段写入。
- 可选增强（如你希望更严谨）：在 `ServiceItemService.insert/update` 增加简单校验（name 必填、price>=0、durationMin>0）。

### 3）前端（新增管理页面 + 表单弹窗）
- 新增页面：`jshERP-web/src/views/cashier/ServiceItemList.vue`
  - mixin 复用 `JeecgListMixin`，设置 `urlPath='/cashier/service_item'`
  - 列表字段：名称、价格、时长(分钟)、启用、排序、操作（编辑/删除）
  - 查询条件：名称（模糊）、启用状态
  - “新增/批量删除/启用/禁用”按钮按 `btnEnableList` 控制
- 新增弹窗：`jshERP-web/src/views/cashier/modules/ServiceItemModal.vue`
  - 表单字段：name、price、durationMin、enabled、sort
  - 提交调用 `cashierAddServiceItem/cashierUpdateServiceItem`
- 不在收银弹窗“项目列表”里直接加新增按钮（避免一线收银误操作）；新增入口放到菜单“服务项目管理”更合适。

### 4）文档
- 在 [docs/使用文档.md](file:///Users/lv/Workspace/erp/docs/使用文档.md) 文末追加“基础资料/收银｜服务项目管理”条目，写清：入口、操作步骤、字段含义、接口与表。

## 验收方式（实现后我会自测）
- 登录“测试用户（租户用户）”进入“服务项目管理”：能看到新增按钮、能新增项目。
- 新增后返回收银弹窗“项目列表”：新项目能被拉取并展示（验证 tenant_id 修复生效）。
- 删除/禁用后收银项目列表不再展示（验证 delete_flag / enabled 过滤生效）。

如果你确认这个方案，我会按上述步骤一次性把前端页面、后端修复、菜单/权限升级脚本、以及使用文档都补齐，并完成联调验证。