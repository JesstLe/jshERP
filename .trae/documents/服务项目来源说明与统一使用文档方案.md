## 服务项目内容在哪
- **数据库来源**：服务项目（收银台“项目列表”）来自表 `jsh_service_item`，建表定义在 [jsh_erp.sql](file:///Users/lv/Workspace/erp/jshERP-boot/docs/jsh_erp.sql#L1044-L1055)。常用字段：`name/price/duration_min/enabled/sort/tenant_id/delete_flag`。
- **后端接口链路**：
  - 列表接口：`GET /cashier/serviceItem/list`，入口在 [ServiceItemController.java](file:///Users/lv/Workspace/erp/jshERP-boot/src/main/java/com/jsh/erp/controller/ServiceItemController.java#L23-L67)，MyBatis SQL 在 [ServiceItemMapper.xml](file:///Users/lv/Workspace/erp/jshERP-boot/src/main/resources/mapper_xml/ServiceItemMapper.xml#L127-L161)（`from jsh_service_item` + `enabled`/`name`/`tenant_id`/`delete_flag` 过滤 + `sort` 排序）。
  - 维护接口已存在：`POST /cashier/serviceItem/add`、`PUT /cashier/serviceItem/update`、`DELETE /cashier/serviceItem/delete`、`DELETE /cashier/serviceItem/deleteBatch`（同文件 [ServiceItemController.java](file:///Users/lv/Workspace/erp/jshERP-boot/src/main/java/com/jsh/erp/controller/ServiceItemController.java#L69-L135)）。
- **前端调用位置**：收银台加载项目列表在 [Cashier.vue](file:///Users/lv/Workspace/erp/jshERP-web/src/views/cashier/Cashier.vue#L333-L373)，接口封装在 [api.js](file:///Users/lv/Workspace/erp/jshERP-web/src/api/api.js#L117-L126)。

## 统一使用文档的落地方式（拟实现）
- **新增一份总文档**：在仓库根目录新建 `docs/使用文档.md`（或 `docs/USAGE.md`，二选一），作为所有二开功能的“单一入口”。
- **文档结构**：
  1. 项目运行与登录（复用现有 README 关键内容，避免重复）
  2. 功能目录（按模块分类的索引）
  3. 功能条目（按时间追加在文档末尾；每次新功能在末尾新增一节）
  4. 功能文档模板（固定字段，方便以后复制粘贴）
- **每个功能条目包含**：功能说明、入口路径/菜单、操作步骤、关键接口（方法+路径+入参/出参要点）、数据表/字段变更、权限/租户影响、注意事项/回滚方式。
- **把本次“开台弹出收银台页面”补进文档末尾**：写清楚从“前台收银 → 选座席 → 开台 → 点项目/点产品 → 结算清台”的操作流程，以及相关接口与新增表 `jsh_cashier_session_product_item`。

## 执行步骤（待你确认后我再动手改）
1. 在仓库根目录创建 `docs/使用文档.md`（并在根 [README.md](file:///Users/lv/Workspace/erp/README.md) 增加一行链接到该文档）。
2. 以“开台弹出收银台页面”为第 1 条追加功能条目。
3. 在文档末尾提供“新增功能条目模板”，后续每次新增功能直接复制模板并追加到尾部。
4. 做一次快速自检：链接是否可点、接口/表名是否与代码一致。